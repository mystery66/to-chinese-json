// 测试模版字符串中文提取
const testTemplate = `已存在此标签值的${window.__BY_STORE?.replaceText}，删除后相关${window.__BY_STORE?.replaceText}将清空这个标签值`;

const anotherTemplate = `用户${userName}在${date}提交了订单`;

const simpleTemplate = `这是一个简单的中文模版字符串`;
